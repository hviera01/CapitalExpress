/// Roles de usuario, iguales a los que usa la app Kotlin original
/// (campo `rol` en la coleccion `usuarios`: "admin" | "cobrador").
class Roles {
  Roles._();

  static const String admin = 'admin';
  static const String cobrador = 'cobrador';
}

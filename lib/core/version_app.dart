/// Versión de la app instalada (Android). Tiene que coincidir con el
/// tag de la GitHub Release (ver ActualizacionService). Se sube en 1
/// cada vez que se publica una release nueva con APK.
const int versionApp = 27;
